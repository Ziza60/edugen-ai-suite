Novo diagnóstico do v5.1.3:

[PPTX][DIAG]
{
  "engine": "export-pptx-v4",
  "engine_version": "5.1.3",
  "status": "blocked",
  "fallback_used": false,
  "cache": "miss",
  "totalSlides": 34,
  "removedSlides": 4,
  "blocking_issues": [
    {
      "slideId": "module_4_slide_3",
      "type": "TECHNICAL_SANITIZATION_DAMAGE",
      "message": "Sintaxe técnica destruída em \"Manipulando Arquivos e Tratando Exceções\" (parênteses vazios após verbo)"
    }
  ]
}

Conclusão:
O QA Veto está funcionando. O problema agora é que o pipeline ainda gera ou sanitiza textos técnicos com parênteses vazios e não consegue reparar automaticamente.

Corrigir de forma estrutural:

1. Instrumentar o pipeline para logar, apenas em modo debug, o conteúdo ANTES e DEPOIS do sanitizer para slides bloqueados por TECHNICAL_SANITIZATION_DAMAGE.

2. Criar repairTechnicalSanitizationDamage(text, moduleTitle, courseTopic, language).

3. Para módulo de arquivos/exceções em Python, preservar ou reconstruir:
- open()
- read()
- write()
- close()
- with open(...)
- try
- except
- finally
- FileNotFoundError
- IOError
- encoding='utf-8'

4. Se o texto ficar com verbo + parênteses vazios, como:
- "usar ()"
- "chamar ()"
- "realizar leitura ()"
- "escrita ()"
- "executar ()"

então:
- tentar restaurar a função provável pelo contexto;
- ou reescrever a frase sem parênteses vazios.

Exemplos:
ruim: "Realize leitura () e escrita ()."
bom: "Realize leitura com read() e escrita com write()."

ruim: "Use () para abrir arquivos."
bom: "Use open() para abrir arquivos."

ruim: "trata erros e para limpeza de recursos."
bom: "Use except para tratar erros e finally para limpeza de recursos."

5. Adicionar dicionário de reparo por domínio:
Python arquivos:
- leitura () → read()
- escrita () → write()
- abrir arquivos → open()
- tratamento de erros → except
- limpeza de recursos → finally

Python POO:
- construtor () → __init__()
- instanciar → Classe(...)
- método () → objeto.metodo()

Python testes:
- classes com e métodos . → classes com unittest.TestCase e métodos test_*
- testes unitários → unittest ou pytest

6. Após repairTechnicalSanitizationDamage:
- reexecutar QA;
- se ainda houver TECHNICAL_SANITIZATION_DAMAGE, bloquear;
- não afrouxar o veto.

Objetivo:
O QA já veta corretamente. Agora o motor deve reparar automaticamente esse tipo recorrente de dano técnico sem exportar PPTX quebrado.

Agora não peça para “relaxar” o QA. Peça para melhorar o reparo técnico.